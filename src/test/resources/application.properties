# Application
spring.application.name=GymTracker-Test

# MongoDB - dedicated test database, never the dev database
spring.data.mongodb.uri=mongodb://localhost:27017/gymtracker_test

# Logging
logging.level.root=WARN
logging.level.com.gymtracker=INFO

# Vaadin
vaadin.launch-browser=false
